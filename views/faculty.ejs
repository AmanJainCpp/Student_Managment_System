<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Faculty Management</title>
    <link rel="stylesheet" href="/stylesheets/style.css"> <!-- Link to your CSS file -->
</head>
<body>
    <header>
        <h1>Faculty Management</h1>
        <nav>
            <ul>
                <li><a href="/admin/dashboard">Dashboard</a></li>
                <li><a href="/admin/faculty/add">Add Faculty</a></li>
                <li><a href="/auth/logout">Logout</a></li>
            </ul>
        </nav>
    </header>
    <main>
        <h2>List of Faculty Members</h2>
        <table>
            <thead>
                <tr>
                    <th>Faculty ID</th>
                    <th>Name</th>
                    <th>Username</th>
                    <th>Email</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                <% faculty.forEach(member => { %>
                    <tr>
                        <td><%= member.faculty_id %></td>
                        <td><%= member.name %></td>
                        <td><%= member.username %></td>
                        <td><%= member.email %></td>
                        <td>
                            <a href="/admin/faculty/edit/<%= member.faculty_id %>">Edit</a>
                            <form action="/admin/faculty/delete" method="POST" style="display:inline;">
                                <input type="hidden" name="id" value="<%= member.faculty_id %>">
                                <button type="submit">Delete</button>
                            </form>
                        </td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </main>
    <footer>
        <p>&copy; 2024 Student Attendance Management System. All rights reserved.</p>
    </footer>
</body>
</html>
